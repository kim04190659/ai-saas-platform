# 自治体クラウド活用、「止まらない仕組み」は最初から設計できる ― Mac miniとOSSで実証したBCP構築

---

## 「Vercelが落ちたら、どうする？」

RunWith Platform の開発を進める中で、ある問いが頭から離れなくなった。

住民からのLINE相談、職員の業務支援、AIによる政策提言——これだけの機能をクラウド上に乗せたとき、**クラウド事業者に障害が発生したら、自治体はどう住民サービスを継続するのか。**

「クラウドは安全だから大丈夫」ではない。大手クラウドでも年に数回は障害が起きる。問題は「障害が起きるかどうか」ではなく「起きたときに何分で復旧できるか」だ。

これが、Sprint #79〜#86（約2週間）で取り組んだテーマだ。

---

## 4層のBCP構造を、段階的に作った

自治体のクラウドBCPに必要な要素を整理すると、4つの層になる。

```
Layer 1：監視     ── 異常をいち早く検知する
Layer 2：バックアップ ── データを守る
Layer 3：フォールバック ── 自動で代替システムに切り替える
Layer 4：オンプレ待機 ── 最終防衛ラインとして物理環境を持つ
```

RunWith では、これをスプリント（1週間単位の開発サイクル）ごとに一つずつ積み上げた。

---

### Layer 1：まず「異常を知る」仕組みを作る（Sprint #79）

クラウド障害への対処は、**検知の速さ**がすべてを決める。

Notionへの接続確認、Vercelのデプロイ状態確認を定期実行するヘルスチェックAPIを実装し、管理画面（`/admin/system-health`）に一元表示した。Vercel Cronで毎朝自動チェックが走り、異常があれば即座に記録される。

コストはゼロ。既存インフラの上に乗せるだけだ。

---

### Layer 2：データを「Supabase」に毎日バックアップする（Sprint #80 / #84）

Notionのデータは便利だが、Notion自体が停止したらアクセスできない。

そこで全自治体の主要3データベース（住民相談・WBコーチング・施策実行記録）を毎日Supabase（PostgreSQL）に自動バックアップする仕組みを実装した。

- バックアップ対象：9自治体 × 3DB = 27タスク
- 実行方式：Vercel Cronで夜間に自動実行
- 保持件数：最新3件（古いものは自動削除）

`curl -X POST /api/admin/notion-backup` を実行したところ、30/30タスクが成功。住民データがクラウド障害時にも失われない状態を確認した。

---

### Layer 3：「自動フォールバック」でシームレスに切り替える（Sprint #81）

バックアップがあっても、切り替えが手動では間に合わない。

`getWithFallback()` という関数を実装した。NotionへのAPIが失敗した瞬間、自動的にSupabaseのバックアップデータに切り替える。職員も住民も気づかないまま、バックアップから情報が提供される。

管理画面には `NotionStatusBanner` コンポーネントを追加。Notionが不調なときは赤いバナーが表示され、担当者に状況を知らせる。

---

### Layer 4：Mac mini × OSSで「オンプレ待機環境」を構築する（Sprint #83〜#86）

ここが今回の核心だ。

**Vercel自体が落ちたとき**、クラウド側のどんな仕組みも機能しない。最終的な防衛ラインとして、**手元の物理マシン（Mac mini）でNext.jsアプリを動かす**環境を構築した。

使ったツールはすべてOSS（オープンソース）：

| ツール | 役割 | コスト |
|--------|------|--------|
| **OrbStack** | Mac上でLinux VMを動かす軽量ツール | 無料（個人利用） |
| **Coolify** | セルフホスト型PaaS。GitHubと連携してアプリを自動デプロイ | 無料（Apache 2.0） |
| **ngrok** | Mac miniをインターネットに安全に公開するトンネル | 無料（固定ドメイン1つ） |

手順は以下の通り：

1. Mac miniにOrbStackをインストールし、Ubuntu VMを起動
2. VM内にCoolifyをインストール（スクリプト1行）
3. GitHubリポジトリをCoolifyに接続してNext.jsアプリをデプロイ
4. ngrokで固定URL（`tubeless-premium-legal.ngrok-free.dev`）を取得
5. LINE DevelopersでWebhook URLをngrok URLに切り替え

これで、Vercelが停止しても**30分以内**にMac miniへの切り替えが完了し、住民からのLINE相談も止まらない状態になった。

かかったコスト：**追加費用ゼロ**（Mac miniは元々所有していたもの）

---

## やってみてわかったこと

**1. BCPはクラウド導入と「同時」に設計すべき**

「あとで考えよう」は禁物だ。データ構造、APIの冪等性、フォールバック先——これらは最初から考慮しないと、後から入れるのが困難になる。

**2. OSSとローカルマシンの組み合わせは強力**

Coolifyは「セルフホスト型Vercel」とも言える。GitHubにpushするだけでMac miniのアプリが自動更新される。クラウドと同じ開発体験をオンプレで再現できた。

**3. LINE Webhookの二重化は必須インフラになる**

自治体が住民との主要な接点をLINEに置いた瞬間、LINEのWebhookは「止めてはいけないインフラ」になる。ngrokによる固定URL取得でこれを解決したが、カスタムドメイン取得後はCloudflare DNS Failoverによる自動切替も視野に入る。

---

## 自治体DX担当者へ

クラウドは確かに便利で安価だ。しかし「クラウドに乗せたから安心」ではない。

住民サービスを止めないためには、**監視・バックアップ・フォールバック・オンプレ待機**の4層を、最初から設計に組み込む必要がある。

今回の実証で見えたのは、「これは大企業にしかできない話ではない」ということだ。Mac mini1台とOSSの組み合わせで、自治体規模のサービスに十分なBCP環境は作れる。

自治体DXを「使う」から「止めない設計で使う」へ。この視点が、これからのクラウド活用に求められると実感している。

---

*RunWith Platform は、自治体向けWell-Being × DX基盤として開発中です。*
*GitHub / Vercel / Supabase / Notion / Claude API を組み合わせたマルチテナント構成で稼働しています。*
